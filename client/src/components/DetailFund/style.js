import styled from "styled-components"

export const DetailFundWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 20px;
  font-size: 1.4rem;
`

export const Fund = styled.h4`
  font-size: 2.6rem;
  font-weight: 400;
`

export const Icon = styled.div`
  display: flex;
  align-items: center;
  cursor: pointer;
  color: var(--text-primary-light);
`
